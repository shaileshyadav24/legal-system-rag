pymongo==4.17.0
python-dotenv==1.2.2
# Kept only for its bundled DefaultEmbeddingFunction (ONNX MiniLM) - must match
# what apps/chat/retrieval.py embeds queries with.
chromadb==1.5.9
pandas==3.0.3
requests==2.34.2
pyarrow==24.0.0
fastparquet==2026.5.0
